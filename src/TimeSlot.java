import java.time.LocalDateTime;
import java.util.Scanner;

public class TimeSlot {
    // Declares two private fields to store start and end times
    private LocalDateTime Datebooked;


    // Constructor that initializes the time slot with start and end times
    public TimeSlot(LocalDateTime Datebooked) {
        this.Datebooked = Datebooked;  // Sets the start time// Sets the end time
    }

}